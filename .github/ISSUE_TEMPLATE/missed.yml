name: Nonna let something through
description: A red suite, a push to main, a secret or a force push got past her.
labels: ["missed"]
body:
  - type: markdown
    attributes:
      value: |
        **Never paste a secret's value here**, even one that already leaked: describe what kind it was and rotate it now.
        If you found a way to get past a gate on purpose (a bypass, not a slip), report it privately instead:
        https://github.com/kapadias/nonna/security/advisories/new
  - type: textarea
    id: what
    attributes:
      label: What got through
      placeholder: The agent said done with tests/test_split.py failing.
    validations:
      required: true
  - type: textarea
    id: action
    attributes:
      label: The command or file involved
      placeholder: '"git push --force origin main" or "config/settings.py"'
      render: text
    validations:
      required: true
  - type: textarea
    id: status
    attributes:
      label: Output of /nonna
      render: text
  - type: dropdown
    id: host
    attributes:
      label: Agent and install
      options: ["Claude Code, plugin", "Claude Code, install.sh", "Codex", "Cursor", "GitHub Copilot", "Gemini CLI", "Windsurf", "Cline", "Kiro", "Other"]
    validations:
      required: true
  - type: input
    id: os
    attributes:
      label: OS and shell
      placeholder: macOS 15, zsh
