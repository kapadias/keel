{
  "name": "nonna",
  "displayName": "Nonna",
  "version": "2.0.0",
  "description": "Your agent says done; Nonna makes it prove it. Runs your test suite before the agent can stop, blocks commits and pushes to main, and blocks secrets in files. Deterministic hooks, no model judges.",
  "author": {
    "name": "Shashank Kapadia",
    "url": "https://github.com/kapadias"
  },
  "homepage": "https://github.com/kapadias/nonna",
  "repository": "https://github.com/kapadias/nonna",
  "license": "MIT",
  "keywords": [
    "claude-code",
    "hooks",
    "tdd",
    "testing",
    "git-hooks",
    "guardrails",
    "secret-scanning",
    "code-review"
  ],
  "userConfig": {
    "run_tests": {
      "type": "boolean",
      "title": "Run your project's tests before the agent can say done",
      "description": "When code changed, Nonna runs the repo's test command at the end of the agent's turn and before a push, and blocks on red. The first session in a repo detects it (pytest, npm test, go test or cargo test) and records it in git config nonna.testCmd; change or clear it there. Turn off to keep only the branch and secret guards.",
      "default": true
    },
    "mode": {
      "type": "string",
      "title": "Default mode",
      "description": "lite: test gate, branch guard, secret guard, short rules. full: adds the docs/STATUS.md gate, the full rules and the develop/main flow. Override per repo with /nonna lite|full|off.",
      "options": ["lite", "full"],
      "default": "lite"
    }
  }
}
